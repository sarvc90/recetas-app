# =============================================================
# Dev profile (local)
# =============================================================
spring.datasource.url=jdbc:mysql://localhost:3306/${DB_NAME_DEV}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER_DEV}
spring.datasource.password=${DB_PASS_DEV}

cors.allowed-origins=http://localhost:80,http://localhost
cors.allowed-origin-patterns=

logging.level.root=INFO
logging.level.com.recetas=DEBUG
